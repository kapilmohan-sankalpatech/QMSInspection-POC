# Core
ultralytics>=8.2.0
opencv-python>=4.9.0
numpy==1.26.4
pyyaml>=6.0
pillow>=10.0.0
matplotlib>=3.8.0

# ONNX / export tooling
onnx>=1.16.0
onnxsim>=0.4.36
onnxruntime-gpu>=1.18.0

# TensorRT (NVIDIA Python wheels; pin to your CUDA version if needed)
tensorrt>=10.0.0
pycuda>=2024.1

# Note: torch/torchvision are installed separately in setup.sh with the
# correct CUDA-specific index URL - do not install from this file alone.
